package models;

public interface AnimalInterface {
    public String getName(); // interface method (does not have a body)
    public int getId(); // interface method (does not have a body)
    public void updateName(String name);
}
